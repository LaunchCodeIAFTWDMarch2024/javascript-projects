

//attendance code: 

const warmup = require('../warmup.js')

describe('testing warmup', function() {
    test('function return should be a string', function() {
        expect(typeof warmup).toBe('string')
    })
})